#!/bin/bash

if [ -z $SPIGOT_BUILD_REV ]; then
    echo "Setting SPIGOT_BUILD_REV to latest"
    SPIGOT_BUILD_REV=latest
fi

cd BuildTools && java -jar BuildTools.jar --rev $SPIGOT_BUILD_REV --compile craftbukkit && cd ..